# ============================================================
# ARQ-RAG TurboQuant — Backend Dependencies
# ============================================================

# --- Core Framework ---
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-dotenv==1.0.1
python-multipart==0.0.12

# --- HTTP & Async ---
httpx==0.27.2
aiofiles==24.1.0

# --- Vector Database ---
qdrant-client==1.12.1

# --- Supabase Storage ---
supabase==2.10.0

# --- LLM & LangChain ---
langchain>=0.3.7
langchain-groq>=0.2.3
langchain-core>=0.3.15
groq>=0.12.0

# --- Embedding (Ollama) ---
# Giao tiếp qua HTTP, không cần thêm package

# --- Numerics & Vector Math ---
numpy>=1.26.4
faiss-cpu>=1.8.0        # K-Means clustering cho Product Quantization

# --- PDF Processing ---
PyMuPDF==1.24.14        # fitz - đọc và parse PDF khoa học

# --- Data Processing ---
pandas==2.2.3
scikit-learn==1.5.2     # dùng cho utilities (normalize, etc.)
scipy==1.14.1

# --- Serialization ---
pickle5; python_version < "3.8"

# --- Utilities ---
tqdm==4.67.1
pydantic==2.10.3
